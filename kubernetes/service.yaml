apiVersion: v1
kind: Service
metadata:
  name: coreapi-product
  namespace: rockstar
  labels:
    app: coreapi-product
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: coreapi-product